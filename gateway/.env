# Sky-Claw Gateway — runtime configuration
# This file MUST NOT be committed to version control (see .gitignore)
# Generate WS_AUTH_TOKEN with: python -c "import secrets; print(secrets.token_hex(32))"
WS_AUTH_TOKEN=REPLACE_ME_WITH_GENERATED_TOKEN
# ALLOWED TELEGRAM USER ID (reference; Telegram auth is handled by TelegramPolling in Python)
ALLOWED_USER_ID=REPLACE_ME_WITH_YOUR_TELEGRAM_USER_ID
