# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Database
*.db
config.toml

# OS
.DS_Store
Thumbs.db

# Testing
.pytest_cache/
htmlcov/
.coverage
coverage.xml

# Temp
/tmp/

# Logs
logs/
local_logs/

# From architectural review
*.log
*.db-shm
*.db-wal
# Agent State (PID Locks)
.run/

# Node.js
node_modules/
gateway/node_modules/
package-lock.json
gateway/package-lock.json

# Gateway secrets (nunca commitear)
gateway/.env
*.token
.env
.skyclaw_backups/

# Debug and test artifacts (may contain secrets)
debug_llm.py
dummy_malicious.py
dummy_*.py

# Generated and accidental Windows output
nul
.kilo/
# Scratch / temporary exploration directories (not part of the project)
scratch/
autoskills_tmp/
scrapskyclaw/
plans/

# Git worktrees (isolated workspaces)
.worktrees/

# Environments and Secrets
**/.env
# Environments and Secrets
**/.env

# Serena MCP data (memories, caches, logs)
.serena/

# Roo Code project config (local IDE configuration)
.roo/

# Antigravity metadata (session state, caches, plans)
.antigravity/*
!/.antigravity/config.json
!/.antigravity/settings.json

# Python tooling caches (must never be tracked — bloat the IDE file-watcher)
.mypy_cache/
.ruff_cache/

# Local-only worktrees (live on disk only; not part of project history)
.claude/worktrees/

# Local agent artifacts (skills/cache/state — keep on disk, untrack from git)
.agents/

# Auditoría y extracción
skyscraping/
project_scrape/
Auditorias/

# Informes y reportes residuales (no código fuente)
INFORME_ANALISIS_ESTATICO.md
informe_revision.md
CODE_REVIEW_RESOLUTION.md
SYNC_PROTOCOL_REPORT.md
REFACTOR-VALIDATION.md
IMPLEMENTATION-SUMMARY.txt
"plan KIMI fase2.txt"

# Kimi agent config
.kimi/

# Agent metadata inside source tree
sky_claw/.kilocode/
sky_claw/.llm-chat-history/

# Backup files
*.backup
