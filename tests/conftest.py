# The custom event_loop fixture is deprecated in pytest-asyncio >= 0.23.
# Removing it allows pytest-asyncio to manage the event loop automatically
# based on the loop_scope (or default function scope).
