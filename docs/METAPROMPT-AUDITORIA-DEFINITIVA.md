Actúa como auditor técnico senior especializado en sistemas multi-agente, seguridad ofensiva, rendimiento de aplicaciones Python/Node.js y dominio de modding de Skyrim SE/AE. Tu objetivo es ejecutar una auditoría integral y definitiva sobre el proyecto Sky-Claw, un sistema de gestión autónoma de mods para Skyrim SE/AE construido sobre un stack dual de Python 3.14+ como daemon principal y Node.js 24 como gateway WebSocket, con aproximadamente 15,000 líneas de código distribuidas en ~40 módulos organizados en 7 capas arquitectónicas, 4 interfaces de usuario, 17 herramientas de agente integradas, y 13 controles de seguridad activos. Debes analizar cada archivo fuente del proyecto y producir un reporte estructurado donde cada hallazgo individual tenga asignada una severidad entre Crítico, Alto, Medio o Bajo, con evidencia concreta del código, referencia al módulo y línea específica, y una recomendación de corrección precisa. El reporte final debe estar organizado por dominios de auditoría y cada hallazgo debe ser reproducible y trazable al código fuente.

Inicia tu auditoría analizando la arquitectura general del sistema. Examina el archivo sky_claw/app_context.py que orquesta el ciclo de vida completo con 443 líneas y dos fases de inicialización secuenciales llamadas start_minimal y start_full. Verifica que start_full sea transaccional: si falla en cualquier punto intermedio tras haber inicializado algunos subsistemas como NetworkGateway, CredentialVault, AsyncModRegistry, SyncEngine o LLMRouter, el sistema no debe quedar en un estado parcial inconsistente. Evalúa si existe un mecanismo de cleanup y rollback en el método stop que revierta parcialmente las inicializaciones completadas. Analiza el acoplamiento fuerte dado que app_context.py importa aproximadamente 20 módulos directamente y determina si el patrón de inicialización secuencial monolítica debería refactorizarse hacia un inicializador por fases con compensación. Examina la separación de responsabilidades entre las 7 capas arquitectónicas: la capa core en sky_claw/core/ con contracts.py, database.py, errors.py, models.py, schemas.py, vfs_orchestrator.py, windows_interop.py y validators/; la capa de agente en sky_claw/agent/ con router.py, semantic_router.py, providers.py, tools_facade.py, context_manager.py, animation_hub.py, autogen_integration.py, lcel_chains.py, purple_security_agent.py y specialized_bridges.py; la capa de datos en sky_claw/db/ con async_registry.py, journal.py, registry.py, rollback_manager.py y snapshot_manager.py; la capa de comunicación en sky_claw/comms/ con frontend_bridge.py, interface.py, telegram_polling.py, telegram_sender.py, telegram.py y ws_daemon.py; la capa de orquestación en sky_claw/orchestrator/ con state_graph.py, supervisor.py, sync_engine.py y ws_event_streamer.py; la capa de herramientas en sky_claw/tools/ con bodyslide_runner.py, dyndolod_runner.py, pandora_runner.py, patcher_pipeline.py, synthesis_runner.py y wrye_bash_runner.py; y la capa de razonamiento en sky_claw/reasoning/ con engine.py, strategies.py, tot.py y types.py. Verifica que no existan dependencias circulares entre capas, que la dirección de dependencia sea consistentemente hacia adentro, y que ningún módulo de una capa interna dependa de una capa externa. Identifica cualquier violación de este principio registrando el módulo, la importación ofensiva y la severidad correspondiente.

Concentra luego tu análisis en el módulo sky_claw/orchestrator/supervisor.py, que con 1588 líneas es el archivo más grande del proyecto y viola gravemente el Principio de Responsabilidad Única. Este módulo acumula responsabilidades de al menos 6 fases de desarrollo incluyendo coordinación de sub-agentes como scraper, tools, interface y database, gestión del ciclo de vida de la sincronización, manejo de eventos WebSocket, orquestación de herramientas externas, y lógica de negocio de mods. Cuenta cuántas responsabilidades distintas puedes identificar, mide la complejidad ciclomática de cada método público, y determina si existen bloques de código duplicado o lógica que debería estar en otros módulos. Evalúa el impacto en testabilidad: dado que el supervisor importa de aproximadamente 15 módulos diferentes, determina si es posible realizar tests unitarios aislados sin mockear toda la cadena de dependencias. Proporciona un plan de refactorización específico indicando qué métodos deben extraerse a qué nuevas clases, manteniendo la compatibilidad funcional.

Analiza la eficiencia y el manejo de errores en todo el código Python. Examina sky_claw/agent/router.py con sus 418 líneas que gestionan el loop de conversación con el LLM, el historial con sliding window de 20 mensajes, la ejecución de tools con un máximo de 10 rondas definido por MAX_TOOL_ROUNDS, y la validación de API keys con detección de placeholders. Verifica que cada excepción posible en el loop de conversación tenga un manejo específico y que no se traguen excepciones silenciosamente con except passthrough sin logging. Revisa sky_claw/agent/providers.py con 338 líneas y 4 providers concretos que implementan el patrón Strategy mediante LLMProvider como clase base abstracta con retry mediante tenacity usando exponential backoff y máximo 5 intentos. Verifica que el retry no enmascare errores permanentes como credenciales inválidas o endpoints incorrectos, y que exista diferenciación entre errores transitorios como rate limiting y errores permanentes como autenticación fallida. Examina la factory function create_provider y verifica que maneje correctamente providers desconocidos sin exponer stack traces al usuario. Revisa cada uno de los 17 tools registrados en sky_claw/agent/tools_facade.py que actúa como Facade, verificando que cada tool tenga manejo de errores propio, que los errores se propaguen correctamente al router, y que no existan paths de ejecución donde un error pueda dejar el sistema en estado inconsistente. Presta especial atención a los tools en sky_claw/agent/tools/system_tools.py que orquestan LOOT, xEdit, FOMOD y análisis de conflictos, ya que estos invocan subprocesses externos que pueden fallar de formas impredecibles.

Examina el código Node.js en gateway/server.js que implementa dos servidores WebSocket en los puertos 18789 y 18790, y en gateway/telegram_gateway.js que implementa el bot de Telegram mediante Grammy. Verifica que el gateway valide correctamente los tokens de autenticación WebSocket usando comparación timing-safe, que no existan race conditions en el manejo de conexiones concurrentes, que los mensajes entrantes sean sanitizados antes de retransmitirlos al daemon Python, y que existan límites de tamaño de mensaje para prevenir ataques de denegación de servicio por memoria. Analiza si el gateway puede degradar silenciosamente a conexiones ws:// sin SSL cuando los certificados no están configurados, lo cual constituiría un riesgo de seguridad Medio identificado en auditorías previas. Verifica que el gateway maneje correctamente la reconexión cuando el daemon Python se reinicia, que no acumule listeners de eventos sin cleanup, y que los errores en una conexión no afecten a otras conexiones activas. Examina el manejo de errores asíncronos en los handlers de Grammy para el bot de Telegram, verificando que los errores de la API de Telegram no causen crashes del proceso Node.js y que exista logging estructurado de errores.

Audita la prevención de conflictos de compatibilidad entre mods de Skyrim, que es el dominio funcional central del sistema. Examina sky_claw/xedit/conflict_analyzer.py con 426 líneas que clasifica conflictos por severidad basándose en record signatures de Skyrim como NPC_, QUST, SCPT, PERK, SPEL, MGEF, FACT, DIAL, PACK para conflictos críticos y CELL, WRLD, REFR, ACHR, NAVM, LAND, WEAP, ARMO para conflictos warning. Verifica que la clasificación sea completa y que no existan record types importantes de Skyrim que no estén cubiertos. Examina sky_claw/xedit/runner.py con 1039 líneas que ejecuta scripts Pascal via SSEEdit en modo headless, verificando que la validación regex de nombres de script y plugins sea estricta suficiente para prevenir inyección de comandos en la línea de argumentos del subprocess. Revisa los scripts Pascal en sky_claw/xedit/scripts/apply_leveled_list_merge.pas y sky_claw/xedit/scripts/list_all_conflicts.pas, verificando que manejen correctamente plugins grandes con miles de records, que no asuman tamaños fijos de buffers, y que el script de merge de leveled lists preserve correctamente las entradas de todos los plugins contribuyentes sin perder datos. Examina sky_claw/assets/asset_scanner.py con 431 líneas que detecta conflictos de assets loose files por prioridad MO2 donde los mods debajo en la lista tienen mayor prioridad, verificando que el algoritmo de escaneo sea correcto para los tipos MESH con extensión .nif, TEXTURE con .dds/.png/.jpg/.tga, SCRIPT con .pex, CONFIG con .ini/.json/.xml, SOUND con .wav/.xwm/.fuz y ANIMATION con .hkx, y que la restricción STRICTLY READ-ONLY se cumpla sin excepciones. Verifica que la integración con LOOT a través de sky_claw/loot/cli.py con 126 líneas genere correctamente el load order, que el parser de masterlist en sky_claw/loot/masterlist.py maneje metadata corrupta o incompleta, y que el output parser en sky_claw/loot/parser.py extraiga correctamente warnings, errores y información de plugins.

Analiza la gestión de memoria y recursos del sistema. Examina las 4 bases de datos SQLite que operan en modo WAL: mod_registry.db como registry principal de mods, los archivos de historial de chat por sesión, journal.db como audit trail de operaciones, y sky_claw_state.db para estado del scraper, memoria del agente y GUI. Verifica que sky_claw/db/async_registry.py con 370 líneas y sky_claw/db/registry.py con 220 líneas no tengan esquemas duplicados con sky_claw/core/database.py con 186 líneas, ya que la duplicación de esquemas entre DatabaseAgent y AsyncModRegistry es un riesgo Alto identificado. Compara las sentencias CREATE TABLE en los tres módulos y reporta cualquier discrepancia en tipos de columnas, constraints o índices. Verifica que el modo WAL esté configurado con busy_timeout de 5000ms en todas las conexiones, que exista PRAGMA quick_check para detectar corrupción, y que las conexiones se cierren correctamente en todos los paths de ejecución incluyendo errores. Examina sky_claw/db/journal.py con 873 líneas que implementa el sistema de journaling con transaction grouping y estados PENDING, COMMITTED y ROLLED_BACK, verificando que no existan leaks de conexiones SQLite, que las transacciones se completen o reviertan atomícamente, y que el journal no crezca indefinidamente sin mecanismo de compactación. Examina sky_claw/db/snapshot_manager.py que realiza backups pre-modificación y sky_claw/db/rollback_manager.py que implementa el motor de undo, verificando que los snapshots no consuman espacio en disco sin control y que exista un límite de snapshots por operación. Analiza el uso de subprocesses en sky_claw/core/vfs_orchestrator.py con 278 líneas que ejecuta herramientas a través del VFS de MO2, verificando que los procesos hijo se terminen correctamente cuando el proceso padre falla, que no existan zombies de proceso acumulados, y que los timeouts sean respetados con cleanup apropiado de recursos. Verifica que sky_claw/mo2/vfs.py con 259 líneas que controla el VFS de MO2 maneje correctamente el ciclo de vida del hook DLL de MO2 y que no deje file locks pendientes.

Audita la integridad de assets del juego y mods. Examina sky_claw/fomod/installer.py con 420 líneas que realiza extracción de archivos y instalación, soportando formatos .zip nativo, .7z requiriendo 7z, y .rar requiriendo rarfile. Verifica que la extracción sea atómica: si falla a mitad de camino, los archivos parciales deben ser limpiados automáticamente mediante el mecanismo de rollback del journal. Examina sky_claw/fomod/parser.py que parsea XML de FOMOD usando defusedxml para prevenir XML bombs, verificando que la validación sea exhaustiva contra payloads maliciosos incluyendo entity expansion attacks, external entity injection, y documentos profundamente anidados. Revisa sky_claw/fomod/resolver.py que resuelve los pasos condicionales del instalador FOMOD, verificando que las condiciones se evalúen correctamente y que no existan paths de ejecución que puedan instalar archivos fuera del directorio destino del mod. Examina sky_claw/fomod/models.py que define los modelos de datos FOMOD, verificando que las validaciones Pydantic sean completas. Verifica que el verificador de integridad SHA256 post-descarga en sky_claw/scraper/nexus_downloader.py con 468 líneas sea criptográficamente correcto, que no existan timing attacks en la comparación de hashes, y que el hash se calcule sobre el archivo completo descargado sin omisiones.

Evalúa la coherencia del lore de Skyrim en las decisiones del agente. Examina el system prompt definido en sky_claw/app_context.py que establece la identidad del agente como Sky-Claw con la regla crítica de responder siempre en español argentino. Verifica que sky_claw/agent/context_manager.py que construye el contexto dinámico del prompt incluya información relevante sobre el dominio de modding de Skyrim incluyendo el reconocimiento de plugins con stripping de extensiones .esp/.esm/.esl, la prioridad de masters .esm mayor que .esl mayor que .esp en load order, y los record types críticos y de warning. Evalúa si el conocimiento de dominio integrado es suficiente para que el agente tome decisiones correctas sobre compatibilidad de mods, o si existen gaps donde el agente podría recomendar combinaciones de mods incompatibles o sugerir acciones que violen las reglas del engine de Skyrim. Verifica que la clasificación de conflictos del ConflictAnalyzer refleje correctamente la semántica del juego, por ejemplo que un conflicto en NPC_ sea genuinamente más crítico que un conflicto en ARMO para la estabilidad del juego. Examina si el SemanticRouter en sky_claw/agent/semantic_router.py que clasifica la intención del usuario usando FastEmbed embeddings con fallback a keyword matching tiene suficiente cobertura para todas las consultas relacionadas con modding de Skyrim que un usuario podría plantear.

Analiza la optimización del rendimiento en todo el sistema. Verifica que todas las operaciones de I/O sean genuinamente asíncronas usando aiosqlite para SQLite, aiohttp para HTTP, aiofiles para filesystem, asyncio.create_subprocess_exec para procesos externos, y asyncio.to_thread para operaciones bloqueantes heredadas. Identifica cualquier llamada bloqueante síncrona en el event loop principal, especialmente en operaciones de filesystem dentro del FOMOD installer, en el parsing de output de xEdit, o en el escaneo de directorios del asset scanner. Examina la configuración de concurrencia: worker_count de 4 en SyncEngine, api_semaphore_limit de 4, batch_size de 20, queue_maxsize de 200, MAX_CONTEXT_MESSAGES de 20 en LLMRouter, MAX_TOOL_ROUNDS de 10 en LLMRouter, y TCP connector limit de 20 en NetworkContext. Evalúa si estos valores son apropiados para el hardware objetivo y si existen cuellos de botella cuando se sincronizan cientos de mods simultáneamente. Verifica que el Circuit Breaker en sky_claw/scraper/masterlist.py con 178 líneas que transiciona entre estados CLOSED, OPEN y HALF-OPEN con umbral de 5 failures y recovery de 60 segundos no cause degradación excesiva bajo carga normal. Examina si DynDOLOD con su timeout de 3600 segundos puede bloquear recursos del sistema innecesariamente y si el VFS orchestrator libera correctamente los recursos durante la generación de LODs.

Audita exhaustivamente la seguridad del sistema evaluando los 13 controles activos y buscando gaps entre ellos. Examina sky_claw/security/network_gateway.py con 245 líneas que implementa el control de egreso con allow-list de dominios incluyendo nexusmods.com y api.telegram.org, private IP blocking para prevención de SSRF, un SafeResolver custom para prevenir DNS rebinding, y un GatewayTCPConnector custom con SSL estricto. Verifica que la allow-list sea exhaustiva y que no existan bypass mediante redirecciones HTTP, URLs con encoding no estándar, o IPv6 mappings de direcciones privadas. Examina sky_claw/core/validators/path.py que implementa la prevención de path traversal con sandbox roots, verificando que no existan bypass mediante symlinks, junction points de Windows, rutas UNC, o encoding de caracteres especiales. Revisa sky_claw/core/validators/ssrf.py que valida URLs contra SSRF, verificando la cobertura contra todas las técnicas conocidas de SSRF incluyendo redirecciones, DNS rebinding, y protocol schemes alternativos. Examina sky_claw/security/hitl.py con 152 líneas que implementa el Human-in-the-Loop guard con timeout y approval flow, verificando que el timeout no pueda ser bypassado y que las aprobaciones sean genuinamente del usuario autorizado. Revisa el CredentialVault en sky_claw/security/credential_vault.py con 149 líneas que usa PBKDF2 y Fernet con salt dinámico por máquina, verificando que la migración desde el salt estático anterior sea segura y que no existan paths donde las credenciales puedan quedar en texto plano en memoria o logs. Examina sky_claw/security/purple_scanner.py con 266 líneas que realiza análisis AST de código, verificando su capacidad para detectar patrones de vulnerabilidad comunes. Revisa la sanitización de prompts en sky_claw/security/sanitize.py contra inyección de prompts al LLM, verificando que los datos de entrada del usuario sean correctamente escapados antes de incluirse en el contexto del prompt. Verifica que la autenticación WebSocket con token-based auth y timeout de 3 segundos en el gateway no sea vulnerable a brute force ni a timing attacks, confirmando que se use crypto.timingSafeEqual. Evalúa la superficie de ataque completa incluyendo la API de Telegram Bot, el WebSocket Gateway, las respuestas de Nexus Mods API, los archivos de mods descargados, los archivos de configuración TOML y JSON, los payloads XML de FOMOD, y los prompts de usuario como vectores de inyección al LLM. Para cada superficie, determina si los controles existentes son suficientes o si existen gaps explotables.

Evalúa la cobertura y calidad del testing. El proyecto tiene aproximadamente 30 archivos de test pero existen gaps significativos: no hay tests para la GUI NiceGUI en sky_claw/gui/app.py con 1286 líneas, no hay tests para el frontend HTML/JS en frontend/, no hay tests para el gateway Node.js en gateway/, no hay tests para el motor de razonamiento Tree-of-Thought en sky_claw/reasoning/engine.py con 637 líneas ni en sky_claw/reasoning/strategies.py con 500 líneas, no hay tests para sky_claw/tools/dyndolod_runner.py con 981 líneas, no hay tests para sky_claw/tools/synthesis_runner.py, no hay tests para sky_claw/tools/wrye_bash_runner.py, y no hay tests end-to-end. Para cada archivo de test existente en el directorio tests/, evalúa la calidad de las aserciones, la cobertura de edge cases, el uso apropiado de fixtures en tests/conftest.py y tests/fixtures/fomod/, y si los tests de integración como test_sync_engine_resilience.py, test_autogen_integration.py, test_patch_integration.py, test_db_integrity.py y test_lcel_stability.py realmente prueban escenarios de integración y no son tests unitarios disfrazados. Determina el porcentaje estimado de cobertura de código considerando los módulos sin tests y califica la robustez del suite de testing.

Verifica la implementación correcta de los patrones de diseño identificados. El patrón Strategy en LLMProvider ABC de sky_claw/agent/providers.py debe tener una interfaz consistente donde todos los providers concretos manejen los mismos tipos de error y normalicen las respuestas al formato interno común. El patrón Facade en AsyncToolRegistry de sky_claw/agent/tools_facade.py debe delegar correctamente a los 17 tools sin introducir lógica de negocio adicional en la fachada. El patrón Circuit Breaker en sky_claw/scraper/masterlist.py debe implementar correctamente las transiciones de estado y el periodo de recuperación. El patrón Producer-Consumer en sky_claw/orchestrator/sync_engine.py con 751 líneas debe manejar correctamente el backpressure cuando la queue de tamaño 200 se llena y los workers de 4 hilos no pueden procesar a la velocidad de producción. El patrón Journal en sky_claw/db/journal.py con 873 líneas debe garantizar durabilidad de las operaciones registradas. El patrón State Machine en sky_claw/orchestrator/state_graph.py con 991 líneas usando LangGraph con SupervisorState enum debe manejar correctamente las transiciones de estado incluyendo estados de error y recuperación. El patrón Tree-of-Thought en sky_claw/reasoning/engine.py con estrategias BFS, DFS, Best-First, Beam Search y MCTS debe implementar correctamente la poda y los límites de profundidad para evitar explosión combinatoria. El patrón Observer en sky_claw/gui/event_bus.py debe evitar memory leaks por listeners no desregistrados. El patrón Contract en sky_claw/core/contracts.py con 410 líneas que implementa SchemaRegistry y decoradores @validate_input y @validate_output con Pydantic debe validar correctamente en ambos bordes de cada función decorada. El patrón Protocol con typing estructural en sky_claw/comms/frontend_bridge.py con 878 líneas para WebSocketClient y UpdateHandler debe ser consistente con las implementaciones concretas.

Analiza las integraciones externas en detalle. Para la integración con Nexus Mods API mediante endpoint https://api.nexusmods.com/v1/games/skyrimspecialedition/ con autenticación por API Key header, verifica que el rate limiting se maneje correctamente con el Circuit Breaker de 5 failures antes de abrir y 60 segundos de recovery, que las descargas verifiquen SHA256 post-download, y que los errores de la API como 404, 429, 500 y timeouts de red se manejen diferenciadamente. Para la integración con Telegram Bot API mediante Grammy en Node.js gateway y aiohttp en Python con Long Polling en desarrollo y Webhook en producción, verifica que los inline keyboard buttons para approve/deny del HITL sean seguros contra falsificación, que el chat ID autorizado se valide en cada mensaje entrante, y que el formato HTML parse mode no sea vulnerable a inyección HTML. Para la integración con LOOT CLI mediante subprocess con argumentos --game SkyrimSE --game-path --sort --update-masterlist y timeout de 60 segundos configurable, verifica que el output parser en sky_claw/loot/parser.py maneje correctamente outputs parciales por timeout y que los paths al ejecutable de LOOT sean validados contra path traversal. Para la integración con xEdit/SSEEdit mediante subprocess headless con scripts Pascal y flag -IKnowWhatImDoing para escritura con timeout de 120 segundos configurable, verifica que la generación dinámica de scripts Pascal en sky_claw/xedit/runner.py no sea vulnerable a inyección de código Pascal, que la validación regex de nombres de script sea suficiente, y que el flag -IKnowWhatImDoing no se pase cuando no se requiera escritura. Para la integración con DynDOLOD/TexGen mediante pipeline multi-etapa de TexGen seguido de DynDOLOD seguido de empaquetado para MO2 ejecutado via VFS Orchestrator con flag -m y timeout de 3600 segundos por defecto, verifica que la validación de output post-ejecución confirme la existencia de archivos .esp y textures generados, y que el empaquetado para MO2 sea correcto. Para las integraciones con Synthesis mediante sky_claw/tools/synthesis_runner.py para pipeline de patchers, Wrye Bash mediante sky_claw/tools/wrye_bash_runner.py para generación de Bashed Patch, Pandora mediante sky_claw/tools/pandora_runner.py para Behavior Engine animations, y BodySlide mediante sky_claw/tools/bodyslide_runner.py para generación de meshes de cuerpo y física, verifica que cada runner maneje correctamente los códigos de salida de la herramienta, que los timeouts sean apropiados para la complejidad de la operación, y que los archivos generados sean validados antes de considerarse exitosos.

Revisa los scripts Pascal en sky_claw/xedit/scripts/apply_leveled_list_merge.pas y sky_claw/xedit/scripts/list_all_conflicts.pas en detalle. Verifica que los scripts manejen correctamente plugins grandes con decenas de miles de records sin asumir tamaños fijos de buffers, que el script de merge de leveled lists preserve todas las entradas de todos los plugins contribuyentes sin perder datos ni duplicar entradas, que el script de listing de conflictos clasifique correctamente los tipos de conflicto según las reglas del engine de Skyrim, y que ambos scripts manejen gracefully los casos donde xEdit no puede cargar un plugin corrupto o donde los plugins tienen masters faltantes.

Audita la configuración y gestión de estado del sistema. Existen dos sistemas de configuración superpuestos: sky_claw/config.py con 265 líneas que maneja configuración TOML con SystemPaths, y sky_claw/local_config.py con 164 líneas que maneja configuración legacy JSON con keyring. Verifica si la migración de JSON a TOML es completa o si existen paths de código que aún lean del formato legacy, si hay inconsistencias entre las opciones disponibles en cada formato, y si el usuario puede quedar en un estado ambiguo donde ambos archivos existen con valores diferentes. Examina el uso de variables de entorno con prefijo SKY_CLAW_* como overrides de configuración, verificando que los tipos de datos se conviertan correctamente y que las variables de entorno tengan prioridad apropiada sobre los archivos de configuración. Revisa el almacenamiento de credenciales que utiliza tres mecanismos: OS Keyring mediante la librería keyring para API keys y tokens, CredentialVault con SQLite y encriptación Fernet para secretos, y las variables de entorno para overrides. Verifica que no existan paths donde las credenciales se almacenen en texto plano, que las API keys del LLM no se logeen accidentalmente, y que el CredentialVault maneje correctamente la rotación de credenciales.

Evalúa la resiliencia y recuperación ante fallos del sistema. Examina el mecanismo de rollback completo: cuando una operación de modificación falla, el OperationJournal en sky_claw/db/journal.py debe tener registradas todas las operaciones realizadas, el FileSnapshotManager en sky_claw/db/snapshot_manager.py debe tener backups pre-modificación, y el RollbackManager en sky_claw/db/rollback_manager.py debe poder restaurar el estado anterior. Verifica que este mecanismo funcione correctamente para cada tipo de operación: instalación de mods, merge de leveled lists, generación de Bashed Patch, generación de LODs con DynDOLOD, y modificación de load order con LOOT. Examina el comportamiento del sistema cuando dependencias opcionales no están disponibles: LangGraph no instalado causa degradación silenciosa que state_graph.py maneja con LANGGRAPH_AVAILABLE, FastEmbed no disponible causa que SemanticRouter pierda capacidad de embeddings pero tiene fallback a keyword matching. Verifica que estas degradaciones sean comunicadas al usuario y no causen comportamiento silencioso incorrecto. Analiza el comportamiento ante corrupción de SQLite en modo WAL bajo carga extrema, verificando que exista PRAGMA quick_check y auto-backup. Examina la resiliencia del SyncEngine en sky_claw/orchestrator/sync_engine.py con su patrón producer-consumer, verificando que un worker que falle no bloquee la queue completa, que los batches parciales se manejen correctamente, y que exista mecanismo de retry para operaciones de API que fallen transitoriamente.

Finalmente, audita la accesibilidad y experiencia de usuario de las 4 interfaces. Para la GUI NiceGUI en sky_claw/gui/app.py con 1286 líneas que incluye dashboard, event_bus, setup_wizard, controllers para chat, mods y navegación, y views con secciones de chat preview, CTA, features, mods preview y stats, verifica que la interfaz sea navegable por teclado, que los elementos interactivos tengan labels accesibles, que el contraste de colores del tema nórdico definido en sky_claw/gui/styles.css sea suficiente para lectibilidad, y que los mensajes de error sean claros y orientados a la acción. Para el frontend web en frontend/ con dashboard.html, index.html, css/medieval-theme.css, css/sky_claw_styles.css, js/app.js y js/dashboard.js, verifica que el HTML sea semántico, que el JavaScript no tenga errores de console, que el tema medieval no comprometa la usabilidad, y que el dashboard funcione correctamente en los navegadores objetivo. Para la interfaz Telegram Bot, verifica que los mensajes tengan formato legible en dispositivos móviles, que los inline keyboards para HITL approval sean intuitivos, y que los mensajes de error no expongan detalles internos del sistema. Para la CLI en sky_claw/__main__.py con 6 modos de operación, verifica que los mensajes de ayuda sean completos, que los códigos de salida sean significativos, y que la interfaz sea usable sin conocimiento previo del sistema.

Para cada hallazgo que reportes, debes incluir obligatoriamente: el módulo y archivo específico donde se encontró, la línea o rango de líneas afectadas, una descripción técnica precisa del problema, la severidad asignada entre Crítico para problemas que pueden causar pérdida de datos, compromisos de seguridad o fallos catastróficos del sistema; Alto para problemas que afectan significativamente la funcionalidad, mantenibilidad o rendimiento; Medio para problemas que degradan la calidad pero no impiden la operación; y Bajo para problemas cosméticos o de best practices, la evidencia concreta del código que sustenta el hallazgo, el impacto potencial si no se corrige, y una recomendación de corrección específica y accionable. Organiza tu reporte por dominios de auditoría en el siguiente orden: primero Seguridad con todos los hallazgos de seguridad ordenados por severidad, luego Arquitectura y Diseño, luego Calidad de Código y Manejo de Errores, luego Rendimiento y Recursos, luego Testing, luego Integraciones Externas, luego Configuración y Estado, luego Dominio de Skyrim y Coherencia de Lore, y finalmente Interfaces y UX. Al inicio del reporte incluye una tabla resumen con el conteo total de hallazgos por severidad y por dominio, y al final incluye una lista priorizada de las 10 correcciones más urgentes que deberían implementarse primero basándose en la combinación de severidad, impacto y facilidad de corrección.